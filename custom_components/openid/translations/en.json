{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_service%]",
      "reconfigure_successful": "[%key:common::config_flow::abort::reconfigure_successful%]",
      "single_instance_allowed": "[%key:common::config_flow::abort::single_instance_allowed%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_cidr": "Enter one valid CIDR block per line",
      "invalid_discovery": "The discovery endpoint did not return the required OpenID endpoints",
      "invalid_ssl_certificate": "TLS certificate validation failed while connecting to the provider",
      "required_fields": "Review the discovered endpoints and fill in any missing values",
      "missing_oidc_metadata": "Issuer and JWKS URL are required when the requested scope includes openid",
      "invalid_tls_configuration": "Keep TLS validation enabled when using a custom CA certificate",
      "invalid_ca_certificate": "The custom CA certificate could not be loaded from the Home Assistant config directory",
      "legacy_oauth_required": "Enable legacy OAuth UserInfo mode when the scope does not include openid",
      "invalid_provider_configuration": "Use valid HTTPS provider endpoints and asymmetric ID-token signing algorithms"
    },
    "step": {
      "advanced": {
        "data": {
          "allow_legacy_oauth": "Allow legacy OAuth UserInfo identity mode",
          "block_login": "Block other login methods",
          "create_user": "Create Home Assistant users automatically",
          "error_url": "Custom error redirect URL",
          "openid_text": "Login button text",
          "trusted_ips_input": "Trusted IPv4/IPv6 CIDR blocks",
          "use_auth_header": "Use HTTP Basic auth for the token request",
          "post_logout_url": "Post-logout redirect URL"
        },
        "data_description": {
          "allow_legacy_oauth": "Permit authentication without the openid scope. This skips ID-token issuer, audience, signature, and nonce validation and should be used only for a trusted legacy OAuth provider.",
          "block_login": "Only allow OpenID login unless the client IP matches one of the trusted IPv4 or IPv6 CIDR blocks below",
          "create_user": "Automatically create a Home Assistant user the first time someone signs in",
          "error_url": "Optional URL to redirect to instead of showing the built-in OpenID error page",
          "openid_text": "Text shown on the Home Assistant login page",
          "trusted_ips_input": "Enter one IPv4 or IPv6 CIDR block per line",
          "use_auth_header": "Send the client ID and secret in the Authorization header when exchanging the code for tokens",
          "post_logout_url": "Optional URL sent to the provider as post_logout_redirect_uri; leave blank to let the provider control the logout flow"
        },
        "description": "Configure the remaining login behavior and defaults."
      },
      "credentials": {
        "data": {
          "client_id": "Client ID",
          "client_secret": "Client secret"
        },
        "data_description": {
          "client_id": "OAuth2/OpenID client ID for Home Assistant",
          "client_secret": "OAuth2/OpenID client secret for Home Assistant"
        },
        "description": "Enter the client credentials Home Assistant should use with the provider."
      },
      "discovery": {
        "data": {
          "configure_url": "Configure URL",
          "validate_tls": "Validate TLS certificate",
          "ca_cert_path": "Custom CA certificate path"
        },
        "data_description": {
          "configure_url": "Discovery endpoint, usually the provider's OpenID configuration URL",
          "validate_tls": "Verify the provider TLS certificate when fetching the discovery document",
          "ca_cert_path": "Optional PEM CA bundle inside the Home Assistant config directory, for example certs/private-ca.pem"
        },
        "description": "Enter the configure URL to discover the provider endpoints automatically."
      },
      "identity": {
        "data": {
          "scope": "Requested scope",
          "username_field": "Username field"
        },
        "data_description": {
          "scope": "OAuth2/OpenID scope to request from the provider",
          "username_field": "Claim to use as the Home Assistant username, for example `preferred_username` or `email`"
        },
        "description": "Choose which claims to request and which user info field should become the Home Assistant username."
      },
      "provider": {
        "data": {
          "authorize_url": "Authorization endpoint",
          "logout_url": "Logout endpoint",
          "token_url": "Token endpoint",
          "use_pkce": "Use PKCE",
          "user_info_url": "User info endpoint",
          "validate_tls": "Validate TLS certificate",
          "issuer": "Issuer",
          "jwks_url": "JWKS endpoint",
          "id_token_signing_algorithms_input": "ID token signing algorithms",
          "ca_cert_path": "Custom CA certificate path"
        },
        "data_description": {
          "authorize_url": "Authorization endpoint",
          "logout_url": "Optional logout endpoint",
          "token_url": "Token endpoint",
          "use_pkce": "Whether the authorization flow should use PKCE",
          "user_info_url": "User info endpoint",
          "validate_tls": "Verify the provider TLS certificate for discovery, token, and user info requests",
          "issuer": "OIDC issuer identifier; required for manual OpenID configuration",
          "jwks_url": "JWKS endpoint used to validate ID token signatures",
          "id_token_signing_algorithms_input": "Comma-separated allowed algorithms, for example RS256 or ES256",
          "ca_cert_path": "Optional PEM CA bundle inside the Home Assistant config directory, for example certs/private-ca.pem"
        },
        "description": "{pkce_message}"
      },
      "reconfigure": {
        "description": "[%key:component::openid::config::step::user::description%]"
      },
      "user": {
        "description": "Choose whether to discover the provider endpoints from a configure URL or enter the endpoint URLs manually.",
        "menu_options": {
          "discovery": "Use configure URL",
          "manual": "Enter URLs manually"
        },
        "title": "Configure provider"
      }
    }
  }
}
